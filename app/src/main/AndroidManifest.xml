<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arzupinarcioglu.phoneabout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneAbout">
        <activity android:name=".ui.activity.DeviceProcessActivity"></activity>
        <activity android:name=".ui.activity.ApplicationDetailsActivity" />
        <activity android:name=".ui.activity.ApplicationsActivity" />
        <activity android:name=".ui.activity.DeviceFeaturesActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>